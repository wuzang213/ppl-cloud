spring:
  redis:
    host: ${redis.host} # 你的redis地址
    port: 6379
    password: ${redis.pw} # 你的redis密码
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s